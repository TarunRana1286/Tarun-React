1. What is ADO.NET and what are its main components?
ADO.NET is a set of managed classes in .NET for accessing and manipulating data from various data sources (databases, XML, etc.). It supports both connected and disconnected data access models.
Main components:

Data Providers: SqlClient (for SQL Server), OleDb, Odbc, etc.
Core Classes: SqlConnection, SqlCommand, SqlDataReader, SqlDataAdapter.
Disconnected Layer: DataSet, DataTable, DataView, DataRelation.

2. How does ADO.NET differ from classic ADO?
Classic ADO (pre-.NET) was COM-based, primarily connected (Recordset kept the connection open), and less scalable for web apps.
ADO.NET is fully managed, supports disconnected architecture (DataSet works offline), offers better performance, XML integration, strong typing, and no COM overhead. It is more suitable for n-tier and web applications.
3. What is the role of the DataSet in ADO.NET?
A DataSet is an in-memory, disconnected cache of data. It can hold multiple DataTables, relationships, constraints, and allows data manipulation without an active database connection. It is useful for batch updates, offline scenarios, and XML serialization.
4. Explain the differences between DataSet and DataReader.

DataReader: Forward-only, read-only, connected (connection must stay open), low memory footprint, high performance. Ideal for reading large result sets once.
DataSet: Disconnected, editable, supports multiple tables/relations, higher memory usage. Ideal when you need to modify data or navigate freely.

5. What are the key classes in ADO.NET?

SqlConnection – Manages database connection.
SqlCommand – Executes SQL/stored procedures.
SqlDataReader – Fast forward-only reader.
SqlDataAdapter – Bridges connected and disconnected models.
DataSet / DataTable – In-memory data containers.
SqlTransaction, SqlParameter, SqlBulkCopy.

6. What is the use of the Connection object in ADO.NET?
The Connection object (SqlConnection) establishes and maintains a session with the data source. Key properties/methods: ConnectionString, Open(), Close(), State. Always wrap in using for proper resource management.
7. How do you handle transactions in ADO.NET?
Use SqlTransaction:
C#using var conn = new SqlConnection(connectionString);
conn.Open();
using var transaction = conn.BeginTransaction();
try
{
    // Execute commands here with transaction passed to commands
    transaction.Commit();
}
catch
{
    transaction.Rollback();
    throw;
}
For distributed transactions, use System.Transactions.TransactionScope.
8. Describe Connection Pooling in ADO.NET and how it can be configured.
Connection Pooling reuses database connections to avoid the overhead of creating new ones. It is enabled by default.
Configure via connection string:
Pooling=true; Max Pool Size=100; Min Pool Size=5; Connection Timeout=30;
9. What is the purpose of Command objects in ADO.NET?
SqlCommand represents a SQL statement or stored procedure to execute against the database. It supports parameters, transactions, and different execution modes (ExecuteNonQuery, ExecuteReader, ExecuteScalar).
10. Can you explain what a DataAdapter does in ADO.NET?
DataAdapter bridges the connected and disconnected worlds. It fills a DataSet/DataTable using Fill() and updates the database with changes using Update(). It internally uses SelectCommand, InsertCommand, UpdateCommand, and DeleteCommand.

Reply with "next" to get Questions 11–20 with full answers and code examples.
This way we can go through all 100 comfortably without overwhelming a single response. Let me know if you want to adjust the batch size or skip to a specific range!https://devinterview.io/questions/web-and-mobile-development/asp-net-mvc-interview-questions/
 
ca you visit this URL and give me QnA for all the 100 questions?Here is the complete list of 100 ASP.NET MVC Interview Questions from the page.
The site lists exactly 100 questions organized into sections. Full detailed answers are behind a paywall — only questions are public. Below, I've extracted every question exactly as it appears and provided detailed, practical answers tailored for .NET / C# developers (covering both classic ASP.NET MVC 5 and modern practices, with relevant notes on ASP.NET Core MVC where the concepts have evolved).
I'll deliver them in batches of 10 questions for better readability. Each answer includes explanations, C# code examples, and interview tips.
1–10: ASP.NET MVC Fundamentals
1. What is ASP.NET MVC and how does it differ from WebForms?
ASP.NET MVC is a web framework that implements the Model-View-Controller pattern for building scalable, testable web applications.
Key differences from WebForms:

MVC is lightweight, follows separation of concerns, supports clean URLs, and is highly testable.
WebForms is event-driven (postbacks, ViewState), heavier, and uses server controls.
MVC gives full control over HTML/HTTP; WebForms abstracts it (often resulting in large ViewState).

2. Explain the MVC architectural pattern.
MVC divides an application into three parts:

Model: Represents data and business logic.
View: Responsible for UI/presentation (Razor views).
Controller: Handles user input, orchestrates Model and View.

It promotes Separation of Concerns, testability, and maintainability.
3. What are the main components of ASP.NET MVC?

Routing (URL mapping)
Controllers & Actions
Views (Razor)
Models / ViewModels
Filters, HTML Helpers, Model Binding
Dependency Injection (in later versions)

4. How does the routing mechanism work in ASP.NET MVC?
Routing maps incoming URLs to controller actions. Configured in RouteConfig.cs (MVC 5) or Program.cs / Startup.cs (Core).
Example (conventional routing):
C#routes.MapRoute(
    name: "Default",
    url: "{controller}/{action}/{id}",
    defaults: new { controller = "Home", action = "Index", id = UrlParameter.Optional }
);
5. What is the role of the Controller in ASP.NET MVC?
The Controller receives HTTP requests, processes input, interacts with the Model, and returns an ActionResult (View, JSON, Redirect, etc.).
It acts as the orchestrator between Model and View.
6. Can you describe the lifecycle of an ASP.NET MVC request?

Request → Routing (finds route)
Controller factory creates Controller
Action filters (Authorization, Action, Result)
Action method executes
Model binding & validation
ActionResult execution (renders View)
Response sent to client

7. What are Actions in ASP.NET MVC?
Actions are public methods in a Controller that handle requests and return ActionResult. They can be decorated with attributes like [HttpGet], [HttpPost], [Authorize].
8. What is Razor View Engine?
Razor is the default view engine in ASP.NET MVC. It uses a clean, compact syntax with @ for C# code inside HTML.
Example:
HTML@model MyViewModel
<h1>@Model.Title</h1>
9. How do you pass data from a Controller to a View?

ViewBag / ViewData (dynamic, for one request)
TempData (survives one redirect)
Strongly-typed Model (recommended)
ViewModel

Example:
C#public ActionResult Index()
{
    ViewBag.Message = "Hello";
    return View(model);
}
10. What are the different ways to manage sessions in ASP.NET MVC?

Session state (HttpContext.Session)
TempData (cookie or session-based)
Cookies
Query strings / Hidden fields
Distributed cache (Redis) for scalability in Core

In modern apps, prefer stateless approaches or distributed caching.

11. Explain the concept of TempData, ViewBag, and ViewData.

ViewData: A Dictionary<string, object> that passes data from Controller to View for the current request only. Type casting is required when reading.

C#// Controller
ViewData["Message"] = "Hello World";
HTML<!-- View -->
<h2>@ViewData["Message"]</h2>

ViewBag: Dynamic wrapper around ViewData (no type casting needed). Also limited to the current request.

C#ViewBag.Message = "Hello World";   // Controller
@ViewBag.Message                    // View

TempData: Uses session (or cookies) to pass data that survives one redirect. Useful for success/error messages after POST-redirect-GET.

C#TempData["Success"] = "Record saved!";
return RedirectToAction("Index");
Key difference: ViewData/ViewBag = current request; TempData = survives one redirect. In ASP.NET Core, prefer ViewData or strongly-typed models over ViewBag for type safety.
12. What are HTML Helpers in ASP.NET MVC?
HTML Helpers are extension methods that generate HTML markup in Razor views, reducing boilerplate and ensuring consistency.
Common helpers:

@Html.TextBoxFor(model => model.Name)
@Html.LabelFor(), @Html.DropDownListFor(), @Html.ValidationMessageFor()
@Html.BeginForm(), @Html.AntiForgeryToken()
@Html.ActionLink()

Example:
HTML@Html.EditorFor(model => model.Email, new { htmlAttributes = new { @class = "form-control" } })
They support strongly-typed binding and automatic validation attributes. In modern ASP.NET Core, Tag Helpers are often preferred over HTML Helpers for cleaner HTML-like syntax.
13. How does Model Binding work in ASP.NET MVC?
Model Binding automatically maps incoming HTTP request data (form fields, query strings, route data, JSON) to action method parameters or model properties.
Process:

DefaultModelBinder looks for matching names.
Uses value providers (Form, QueryString, RouteData, etc.).
Performs type conversion and validation.

Example:
C#[HttpPost]
public ActionResult Create(UserViewModel model)  // model properties auto-populated
{
    if (!ModelState.IsValid) return View(model);
    // save
}
Custom model binders can be created by implementing IModelBinder for complex scenarios (e.g., binding to entities with relationships).
14. What is the purpose of the _ViewStart file in ASP.NET MVC?
_ViewStart.cshtml is executed before every view is rendered. It allows setting common layout or other view-wide settings.
Typical content:
HTML@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
You can add conditional logic here (e.g., different layouts based on user role). It runs for all views unless overridden in the individual view with @Layout = null; (for partial views).
15. What are Partial Views and how are they different from View Components?
Partial Views:

Reusable Razor markup (.cshtml).
Rendered using @Html.Partial("_PartialView") or @Html.RenderPartial().
Can accept a model: @Html.Partial("_LoginPartial", model).
No separate controller logic.

View Components (introduced in MVC 6 / Core):

More powerful and self-contained.
Have their own C# class (inherits ViewComponent) with Invoke() or InvokeAsync() method.
Can have logic, dependency injection, and async support.
Rendered with <vc:component-name></vc:component-name> or @await Component.InvokeAsync("ComponentName", parameters).

Difference: Partial Views are simple markup reuse. View Components are like mini-controllers for complex reusable UI (e.g., sidebar, cart summary) with business logic.
16. Explain Attribute Routing in ASP.NET MVC.
Attribute Routing allows defining routes directly on controllers and actions using attributes (introduced in MVC 5).
Advantages over conventional routing:

More precise control
Better for RESTful APIs
Supports constraints and defaults easily

Example:
C#[Route("products")]
public class ProductsController : Controller
{
    [Route("")]                    // /products
    [Route("{id:int}")]            // /products/5
    public ActionResult Details(int id) { ... }
    
    [Route("search/{term?}")]      // Optional parameter
    public ActionResult Search(string term) { ... }
}
In ASP.NET Core, attribute routing is the default/recommended approach and is more feature-rich.
17. How do you implement Forms Authentication in ASP.NET MVC?
Forms Authentication uses cookies to track authenticated users.
Steps (MVC 5):

Configure in web.config:

XML<authentication mode="Forms">
  <forms loginUrl="~/Account/Login" timeout="2880" />
</authentication>

Use [Authorize] attribute on controllers/actions.
In Login action:

C#var ticket = new FormsAuthenticationTicket(...);
var cookie = new HttpCookie(FormsAuthentication.FormsCookieName, FormsAuthentication.Encrypt(ticket));
Response.Cookies.Add(cookie);
In ASP.NET Core, Forms Authentication is replaced by Cookie Authentication + ASP.NET Core Identity (much cleaner with SignInManager and UserManager).
18. What is the AntiForgeryToken() helper and how does it work?
@Html.AntiForgeryToken() generates a hidden form field and cookie to prevent Cross-Site Request Forgery (CSRF) attacks.
How it works:

Server generates a unique token (stored in cookie + hidden field).
On POST, the framework validates that the token in the form matches the cookie token.

Usage:
HTML@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    ...
}
On action: No extra code needed — the framework checks automatically. In Core, it's @Html.AntiForgeryToken() or the [ValidateAntiForgeryToken] attribute.
19. What is an Area in ASP.NET MVC and how is it used?
Areas allow partitioning a large application into smaller, independent sections (e.g., Admin, Blog, Shop), each with its own controllers, views, and models.
Setup:

Right-click project → Add → Area → "Admin"
Creates Areas/Admin/Controllers, Views, Models

Routing (in AreaRegistration.cs or attribute routing):
C#[Area("Admin")]
public class DashboardController : Controller { ... }
URL example: /Admin/Dashboard/Index
Areas help with organization, routing isolation, and team collaboration on large projects.
20. How do you implement error handling in ASP.NET MVC?
Multiple levels:

Action-level: Use try-catch in actions or [HandleError] attribute.
Controller-level: Override OnException in base controller.
Global-level: In FilterConfig.cs register HandleErrorAttribute.
Custom error pages: Configure in web.config:

XML<customErrors mode="On" defaultRedirect="~/Error/Error">
  <error statusCode="404" redirect="~/Error/NotFound" />
</customErrors>
In ASP.NET Core: Use middleware (app.UseExceptionHandler("/Error")) or IExceptionFilter.
Best practice: Log exceptions (using Serilog/ELMAH) and show user-friendly messages without leaking sensitive info.

21. What is Dependency Injection and how do you implement it in ASP.NET MVC?
Dependency Injection (DI) is a design pattern where dependencies (services, repositories, etc.) are injected into a class rather than being created inside it. It promotes loose coupling, testability, and follows the Dependency Inversion Principle (DIP).
In ASP.NET MVC 5:

MVC does not have built-in DI. You typically use third-party containers like Unity, Ninject, Autofac, or Simple Injector.
Implement via a custom IDependencyResolver or controller factory.

Example with Autofac (common approach):
C#// In Global.asax.cs
protected void Application_Start()
{
    var builder = new ContainerBuilder();
    builder.RegisterControllers(typeof(MvcApplication).Assembly);
    builder.RegisterType<UserRepository>().As<IUserRepository>();
    
    var container = builder.Build();
    DependencyResolver.SetResolver(new AutofacDependencyResolver(container));
}
In ASP.NET Core MVC, DI is built-in (using IServiceCollection in Program.cs):
C#builder.Services.AddScoped<IUserRepository, UserRepository>();
builder.Services.AddControllersWithViews();
Then inject in controller:
C#public class UserController : Controller
{
    private readonly IUserRepository _repo;
    public UserController(IUserRepository repo) => _repo = repo;
}
Best practice: Always program against interfaces.
22. Explain the concept of Action Filters.
Action Filters are attributes that allow you to execute code before or after an action method runs. They enable cross-cutting concerns like logging, authorization, exception handling, etc.
Types of filters (in order of execution):

Authorization filters (IAuthorizationFilter)
Action filters (IActionFilter – OnActionExecuting, OnActionExecuted)
Result filters (IResultFilter)
Exception filters (IExceptionFilter)

Built-in examples:

[Authorize]
[HandleError]
[OutputCache]
[ValidateAntiForgeryToken]

Custom filter example:
C#public class LoggingFilterAttribute : ActionFilterAttribute
{
    public override void OnActionExecuting(ActionExecutingContext filterContext)
    {
        // Log before action
        base.OnActionExecuting(filterContext);
    }
}
Apply globally, at controller, or action level. In ASP.NET Core, filters are more powerful with IAsyncActionFilter for async support.
23. How do you implement AJAX in ASP.NET MVC?
AJAX allows partial page updates without full postback.
Common ways:

Using jQuery AJAX (most popular):

JavaScript$.ajax({
    url: '/User/GetUser/5',
    type: 'GET',
    success: function(data) { $('#result').html(data); }
});

Using Ajax helper (MVC 5):

HTML@Ajax.ActionLink("Load User", "GetUser", new AjaxOptions { UpdateTargetId = "resultDiv", InsertionMode = InsertionMode.Replace })

Return JsonResult from action:

C#public JsonResult GetUser(int id)
{
    var user = _repo.Get(id);
    return Json(user, JsonRequestBehavior.AllowGet);
}
In modern apps, prefer fetch API or Axios + return JsonResult or use Razor Pages / Blazor for better separation.
24. What are custom model binders and when would you use them?
Custom Model Binders allow you to control how model binding happens for complex or non-standard types.
When to use:

Binding to custom types (e.g., DateTime with specific format)
Binding from multiple sources
Complex object graphs with special logic

Implementation:
C#public class CustomDateTimeBinder : IModelBinder
{
    public Task BindModelAsync(ModelBindingContext bindingContext)
    {
        // Custom parsing logic
        var value = bindingContext.ValueProvider.GetValue(bindingContext.ModelName).FirstValue;
        if (DateTime.TryParseExact(value, "dd/MM/yyyy", ...))
        {
            bindingContext.Result = ModelBindingResult.Success(parsedDate);
        }
        return Task.CompletedTask;
    }
}
Register it globally or with [ModelBinder(BinderType = typeof(CustomDateTimeBinder))] attribute.
25. What is an ActionResult and what derived classes does it have?
ActionResult is an abstract base class that represents the result of an action method. The framework executes it to produce the final response.
Common derived classes:

ViewResult – Renders a Razor view
PartialViewResult – Renders a partial view
JsonResult – Returns JSON
RedirectResult / RedirectToActionResult / RedirectToRouteResult
ContentResult – Returns plain text/content
FileResult / FileContentResult / FileStreamResult
HttpStatusCodeResult / HttpNotFoundResult
EmptyResult

In ASP.NET Core, many are unified under IActionResult.
Example:
C#public ActionResult Index() => View();                    // ViewResult
public ActionResult GetData() => Json(data);              // JsonResult
public ActionResult Download() => File(bytes, "application/pdf");
26. Explain the WebAPI and how it differs from traditional MVC Controllers.
ASP.NET Web API is a framework for building HTTP services (RESTful APIs) that can be consumed by any client (mobile, SPA, etc.).
Key differences from MVC Controllers:

Web API returns data (JSON/XML), not views.
Uses ApiController base class (in Core) vs Controller.
Content negotiation (chooses format based on Accept header).
No ViewBag, Razor, or HTML helpers.
Better support for HTTP verbs ([HttpGet], [HttpPost], etc.).
Stateless by design.

In ASP.NET Core, MVC and Web API are unified — the same Controller class can return views or JSON.
27. Describe the role of Global.asax in ASP.NET MVC?
Global.asax is the application-level code file. It contains event handlers for the application lifecycle.
Important events:

Application_Start() – Runs once when app starts (register routes, filters, bundles, DI)
Application_End()
Session_Start() / Session_End()
Application_Error() – Global error handling
Application_BeginRequest() / EndRequest()

In ASP.NET Core, Global.asax is replaced by Program.cs and Startup.cs (or minimal hosting model).
28. How do you handle caching in ASP.NET MVC?
Caching improves performance by storing frequently accessed data.
Types:

Output Caching: [OutputCache(Duration = 60, VaryByParam = "id")] on action or controller.
Data Caching: Use MemoryCache or HttpRuntime.Cache (MVC 5).
Donut Caching / Fragment Caching with partial views.
Client-side caching via HTTP headers (Cache-Control).

In ASP.NET Core:

Built-in IMemoryCache and IDistributedCache (Redis/SQL Server).
Response Caching Middleware.

Best practice: Cache at multiple levels (data, output, client) and invalidate properly.
29. What are Bundling and Minification and how do they improve performance?
Bundling: Combines multiple CSS/JS files into a single file to reduce HTTP requests.
Minification: Removes whitespace, comments, shortens variable names to reduce file size.
In MVC 5 (in BundleConfig.cs):
C#bundles.Add(new ScriptBundle("~/bundles/jquery").Include(
    "~/Scripts/jquery-{version}.js"));

bundles.Add(new StyleBundle("~/Content/css").Include("~/Content/site.css"));
Rendered in layout:
HTML@Scripts.Render("~/bundles/jquery")
@Styles.Render("~/Content/css")
Benefits: Fewer requests, smaller payload → faster page load.
In ASP.NET Core, bundling/minification is usually handled by Webpack, Vite, Gulp, or built-in WebOptimizer / Bundler & Minifier extension.
30. Explain the differences between GET and POST actions in ASP.NET MVC.

31. What is the purpose of the [Authorize] attribute?
The [Authorize] attribute restricts access to authenticated users only. It is an authorization filter that checks if the user is logged in before executing the action.
C#[Authorize]
public class AdminController : Controller { ... }

// Action level
[Authorize(Roles = "Admin, Manager")]
public ActionResult Delete(int id) { ... }

Without parameters: Requires authentication only.
With Roles: Checks specific roles.
With Users: Specific usernames (rarely used).

In ASP.NET Core, it works similarly but integrates with the new Identity system and policy-based authorization:
C#[Authorize(Policy = "RequireAdmin")]
It can be applied at controller, action, or globally.
32. How do you implement custom authorization in ASP.NET MVC?
Create a custom attribute by inheriting from AuthorizeAttribute and overriding AuthorizeCore() or HandleUnauthorizedRequest().
Example:
C#public class CustomAuthorizeAttribute : AuthorizeAttribute
{
    protected override bool AuthorizeCore(HttpContextBase httpContext)
    {
        // Custom logic - e.g., check IP, subscription, claims, etc.
        return httpContext.User.Identity.IsAuthenticated 
               && HasValidSubscription(httpContext.User);
    }

    protected override void HandleUnauthorizedRequest(AuthorizationContext filterContext)
    {
        filterContext.Result = new RedirectResult("~/Account/AccessDenied");
    }
}
Usage: [CustomAuthorize]
In ASP.NET Core, prefer Policy-based authorization with IAuthorizationHandler for more flexible and testable logic.
33. What are Child Actions and how are they different from Partial Views?
Child Actions are special action methods marked with [ChildActionOnly] that can be rendered inside a parent view.
C#[ChildActionOnly]
public ActionResult Menu()
{
    var menuItems = _menuService.GetMenu();
    return PartialView("_Menu", menuItems);
}
In View:
HTML@Html.Action("Menu")   // or @Html.RenderAction("Menu")
Difference from Partial Views:

Partial Views: Pure markup (no controller logic).
Child Actions: Have their own action method, can have business logic, model binding, and filters.

Note: Child Actions are deprecated in ASP.NET Core. Use View Components instead (much better performance and async support).
34. Explain how Validation works in ASP.NET MVC.
Validation is handled through Data Annotations and ModelState.

Add validation attributes to model properties:

C#public class UserViewModel
{
    [Required(ErrorMessage = "Name is required")]
    [StringLength(50, MinimumLength = 3)]
    public string Name { get; set; }

    [EmailAddress]
    [Required]
    public string Email { get; set; }
}

In controller:

C#if (!ModelState.IsValid)
    return View(model);   // returns validation errors to view

In View (Razor):

HTML@Html.ValidationSummary(true)
@Html.ValidationMessageFor(model => model.Email)
Client-side validation is enabled automatically via jquery.validate and unobtrusive validation.
35. What is ModelState and how is it used?
ModelState is a dictionary that holds the state of model binding and validation.

It contains errors for each property.
ModelState.IsValid tells whether the model passed all validations.
You can manually add errors: ModelState.AddModelError("Email", "Email already exists");

It is automatically populated during model binding and is passed to the View so validation messages can be displayed.
36. How do you create a custom validation attribute?
Inherit from ValidationAttribute and override IsValid():
C#public class AgeValidationAttribute : ValidationAttribute
{
    private readonly int _minimumAge;

    public AgeValidationAttribute(int minimumAge)
    {
        _minimumAge = minimumAge;
    }

    protected override ValidationResult IsValid(object value, ValidationContext validationContext)
    {
        if (value is int age && age < _minimumAge)
            return new ValidationResult($"Age must be at least {_minimumAge}.");

        return ValidationResult.Success;
    }
}
Usage:
C#[AgeValidation(18)]
public int Age { get; set; }
In ASP.NET Core, you can also implement IAsyncModelValidator for async validation.
37. What are Display and Editor Templates?
Display Templates (DisplayFor): Control how a property is rendered when displaying data.
Editor Templates (EditorFor): Control how a property is rendered in edit forms.
Create templates in Views/Shared/DisplayTemplates or EditorTemplates folders.
Example – Boolean.cshtml (Editor Template):
HTML@model bool?
@Html.CheckBox("", Model ?? false, new { @class = "form-check-input" })
Then:
HTML@Html.EditorFor(model => model.IsActive)   // automatically uses Boolean template
This helps maintain consistent UI across the application.
38. Explain the concept of Scaffolding in ASP.NET MVC.
Scaffolding is a code-generation feature in Visual Studio that automatically generates CRUD controllers and views based on a model.
How to use:

Add a model class.
Right-click Controllers folder → Add → Controller → "MVC Controller with views, using Entity Framework".
Select Model and DbContext.

It generates:

Controller with Index, Create, Edit, Delete, Details actions
Corresponding Razor views

Useful for rapid prototyping. In modern development, many developers prefer manual implementation or use tools like CRUD generators with better patterns.
39. How do you implement logging in ASP.NET MVC applications?
Common approaches:

ELMAH (legacy, good for error logging)
log4net or NLog
Serilog (most popular today)
Application Insights (Azure)

Example with Serilog in MVC 5 (via Global.asax):
C#Log.Logger = new LoggerConfiguration()
    .WriteTo.File("logs/log.txt")
    .WriteTo.ApplicationInsights(...)
    .CreateLogger();
In controller:
C#private readonly ILogger _logger;

public HomeController(ILogger<HomeController> logger)
{
    _logger = logger;
}

public ActionResult Index()
{
    _logger.Information("Home page visited");
    ...
}
In ASP.NET Core, logging is built-in with ILogger<T> injected automatically.
40. What is the difference between TempData and Session?



































FeatureTempDataSessionLifetimeSurvives only one redirectSurvives until session expiresStorageUsually Session or CookieServer-side (InProc, StateServer, SQL, Redis)Use CaseSuccess/Error messages after POSTUser-specific data across multiple requestsSize LimitSmall dataLarger data (but careful with memory)Type Safetyobject (needs casting)object (needs casting)
Recommendation: Use TempData for one-time messages. Prefer distributed cache or database for larger session data in scalable apps.













































AspectGETPOSTPurposeRetrieve dataSubmit/create/update dataData in URLVisible (query string)Not visible (in body)IdempotentYes (safe to repeat)NoSecurityLess secure for sensitive dataBetter for sensitive dataCachingCan be cachedShould not be cachedBookmarkableYesNoAttributes[HttpGet] (default)[HttpPost]
Use GET for read operations and POST for write operations. Always use anti-forgery token with POST.